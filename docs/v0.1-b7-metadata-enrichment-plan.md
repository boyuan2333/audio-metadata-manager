# v0.1-b7 Metadata Enrichment 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 新增 `app.py enrich`，把 `library.json`、已有 `embeddings.json` 和受控 semantic vocabulary 合并为 `library.enriched.json`。

**架构：** 新增 `audio_metadata.enrichment` 作为纯业务模块，新增 `enrich_cli.py` 作为 CLI 适配层，并在 `app.py` 注册命令。embedding 不写入主 JSON，只写状态、引用和 semantic tags。

**技术栈：** Python、argparse、JSON、pytest、现有 `audio_metadata.schema`、现有 `audio_metadata.clap_embed.cosine_similarity`。

---

## 文件结构

### 新增文件

- `audio_metadata/enrichment.py`
  - 负责 enrichment 纯逻辑。
  - 不直接解析 CLI 参数。
  - 不直接调用真实 CLAP 模型。
  - 输入 payload、embeddings data、vocabulary data，输出 enriched payload 和 summary。

- `enrich_cli.py`
  - 负责 argparse 参数、文件读写、dry-run、force 校验。
  - 调用 `audio_metadata.enrichment.enrich_payload()`。

- `tests/test_enrichment.py`
  - 单元测试 enrichment 纯逻辑。
  - 使用假 embedding 和假 vocabulary。

- `tests/test_enrich_cli.py`
  - 测试 CLI 行为。
  - 覆盖 dry-run、force、坏 JSON、正常写入。

- `config/semantic_tags.json`
  - 默认 semantic vocabulary。
  - MVP 使用少量高价值标签。

### 修改文件

- `app.py`
  - import `enrich_cli as enrich_command`。
  - 注册 `enrich` 子命令。

- `audio_metadata/schema.py`
  - 扩展 `RETRIEVAL_FIELD_DEFAULTS`。
  - 扩展 `MODEL_OUTPUT_FIELD_DEFAULTS`。
  - 保持 schema v1 兼容，不 bump schema version。

- `README.md`
  - 增加 v0.1-b7 `enrich` 使用说明。

- `CURRENT_GOAL.md`
  - 更新当前目标为 v0.1-b7。

---

## 任务 1：扩展 schema 默认字段

**文件：**

- 修改：`audio_metadata/schema.py`
- 测试：`tests/test_schema_model_outputs.py`

- [ ] **步骤 1：编写失败的 schema 测试**

在 `tests/test_schema_model_outputs.py` 增加测试：

```python
def test_schema_v1_includes_enrichment_defaults():
    from audio_metadata.schema import normalize_record_schema_v1

    record = normalize_record_schema_v1({
        "source": {
            "path": "audio/warm_guitar.wav",
            "file_name": "warm_guitar.wav",
            "file_format": "wav",
        },
        "status": "ok",
    })

    assert record["retrieval"]["semantic_tags"] == []
    assert record["retrieval"]["embedding_ref"] is None
    assert record["retrieval"]["embedding_model"] is None
    assert record["retrieval"]["embedding_status"] is None
    assert record["model_outputs"]["semantic_tags"] == []
    assert record["model_outputs"]["semantic_tags_confidence"] == {}
```

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_schema_model_outputs.py::test_schema_v1_includes_enrichment_defaults -v
```

预期：FAIL，失败原因是 `semantic_tags` 或 `embedding_status` 字段不存在。

- [ ] **步骤 3：实现最少 schema 扩展**

在 `audio_metadata/schema.py` 中修改：

```python
RETRIEVAL_FIELD_DEFAULTS: dict[str, Any] = {
    "tags": [],
    "mood": None,
    "texture": None,
    "density": None,
    "role": None,
    "domain": None,
    "semantic_tags": [],
    "embedding_ref": None,
    "embedding_model": None,
    "embedding_status": None,
}
```

并在 `MODEL_OUTPUT_FIELD_DEFAULTS` 增加：

```python
"semantic_tags": [],
"semantic_tags_confidence": {},
```

注意：当前文件已有 `subjective_tags`，不要删除。

- [ ] **步骤 4：运行测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_schema_model_outputs.py::test_schema_v1_includes_enrichment_defaults -v
```

预期：PASS。

- [ ] **步骤 5：Commit**

```bash
git add audio_metadata/schema.py tests/test_schema_model_outputs.py
git commit -m "feat(schema): 添加 v0.1-b7 元数据增强默认字段"
```

---

## 任务 2：实现 embedding 匹配与状态写入

**文件：**

- 创建：`audio_metadata/enrichment.py`
- 创建：`tests/test_enrichment.py`

- [ ] **步骤 1：编写失败测试：未提供 embeddings 时标记 `not_requested`**

创建 `tests/test_enrichment.py`：

```python
from audio_metadata.enrichment import enrich_payload


def _payload_one_record():
    return {
        "schema_version": 1,
        "app_version": "test",
        "run": {},
        "files": [
            {
                "id": "rec-1",
                "status": "ok",
                "source": {
                    "path": "audio/warm_guitar.wav",
                    "file_name": "warm_guitar.wav",
                    "file_format": "wav",
                },
            }
        ],
    }


def test_enrich_marks_embedding_not_requested_without_embeddings():
    enriched, summary = enrich_payload(_payload_one_record())

    record = enriched["files"][0]
    assert record["retrieval"]["embedding_status"] == "not_requested"
    assert record["retrieval"]["embedding_ref"] is None
    assert summary["records"] == 1
    assert summary["embeddings_not_requested"] == 1
```

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_marks_embedding_not_requested_without_embeddings -v
```

预期：FAIL，失败原因是 `audio_metadata.enrichment` 模块不存在。

- [ ] **步骤 3：实现最少 `enrich_payload()`**

创建 `audio_metadata/enrichment.py`：

```python
from __future__ import annotations

from copy import deepcopy
from typing import Any

from audio_metadata.schema import normalize_payload_schema_v1

EMBEDDING_MODEL_DEFAULT = "laion-audioclip-full-2022"
EMBEDDING_DIM_DEFAULT = 512


def enrich_payload(
    payload: dict[str, Any],
    *,
    embeddings_data: dict[str, Any] | None = None,
    vocabulary_data: dict[str, Any] | None = None,
    semantic_tags: bool = False,
    threshold: float = 0.25,
    top_n: int = 5,
) -> tuple[dict[str, Any], dict[str, Any]]:
    enriched = normalize_payload_schema_v1(deepcopy(payload))
    records = enriched["files"]
    summary = {
        "records": len(records),
        "embeddings_ready": 0,
        "embeddings_missing": 0,
        "embeddings_invalid": 0,
        "embeddings_not_requested": 0,
        "semantic_tags_added": 0,
    }

    if embeddings_data is None:
        for record in records:
            record["retrieval"]["embedding_status"] = "not_requested"
        summary["embeddings_not_requested"] = len(records)
        return enriched, summary

    return enriched, summary
```

- [ ] **步骤 4：运行测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_marks_embedding_not_requested_without_embeddings -v
```

预期：PASS。

- [ ] **步骤 5：编写失败测试：path 完全匹配时为 `ready`**

在 `tests/test_enrichment.py` 增加：

```python
def test_enrich_marks_embedding_ready_when_path_matches():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {
                "path": "audio/warm_guitar.wav",
                "embedding": [0.1] * 512,
            }
        ],
    }

    enriched, summary = enrich_payload(
        _payload_one_record(),
        embeddings_data=embeddings_data,
    )

    record = enriched["files"][0]
    assert record["retrieval"]["embedding_status"] == "ready"
    assert record["retrieval"]["embedding_model"] == "laion-audioclip-full-2022"
    assert record["retrieval"]["embedding_ref"] == "embeddings.json#audio/warm_guitar.wav"
    assert summary["embeddings_ready"] == 1
```

- [ ] **步骤 6：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_marks_embedding_ready_when_path_matches -v
```

预期：FAIL，失败原因是状态未写入 `ready`。

- [ ] **步骤 7：实现 embedding lookup 与 ready 状态**

在 `audio_metadata/enrichment.py` 增加：

```python
def _build_embedding_lookup(embeddings_data: dict[str, Any]) -> dict[str, dict[str, Any]]:
    lookup: dict[str, dict[str, Any]] = {}
    for item in embeddings_data.get("files", []):
        if not isinstance(item, dict):
            continue
        path = item.get("path")
        if isinstance(path, str):
            lookup[path] = item
    return lookup


def _record_path(record: dict[str, Any]) -> str | None:
    source = record.get("source") if isinstance(record.get("source"), dict) else {}
    path = source.get("path")
    return path if isinstance(path, str) else None
```

并在 `enrich_payload()` 的 `embeddings_data is not None` 分支中实现：

```python
embedding_lookup = _build_embedding_lookup(embeddings_data)
model_name = embeddings_data.get("model") or EMBEDDING_MODEL_DEFAULT

for record in records:
    path = _record_path(record)
    item = embedding_lookup.get(path or "")
    if item is None:
        record["retrieval"]["embedding_status"] = "missing"
        summary["embeddings_missing"] += 1
        continue

    record["retrieval"]["embedding_status"] = "ready"
    record["retrieval"]["embedding_model"] = model_name
    record["retrieval"]["embedding_ref"] = f"embeddings.json#{item['path']}"
    summary["embeddings_ready"] += 1
```

- [ ] **步骤 8：运行相关测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py -v
```

预期：当前两个测试 PASS。

- [ ] **步骤 9：Commit**

```bash
git add audio_metadata/enrichment.py tests/test_enrichment.py
git commit -m "feat(enrichment): 添加 embedding coverage 状态写入"
```

---

## 任务 3：补齐 embedding 缺失、无效和文件名匹配

**文件：**

- 修改：`audio_metadata/enrichment.py`
- 修改：`tests/test_enrichment.py`

- [ ] **步骤 1：编写失败测试：缺失 embedding 时为 `missing`**

```python
def test_enrich_marks_embedding_missing_when_no_match():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {"path": "audio/other.wav", "embedding": [0.1] * 512}
        ],
    }

    enriched, summary = enrich_payload(_payload_one_record(), embeddings_data=embeddings_data)

    record = enriched["files"][0]
    assert record["retrieval"]["embedding_status"] == "missing"
    assert summary["embeddings_missing"] == 1
```

- [ ] **步骤 2：运行测试验证失败或确认当前行为**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_marks_embedding_missing_when_no_match -v
```

预期：如果已 PASS，继续下一步；如果 FAIL，修复缺失状态逻辑。

- [ ] **步骤 3：编写失败测试：维度错误时为 `invalid`**

```python
def test_enrich_marks_embedding_invalid_when_dimension_is_wrong():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {"path": "audio/warm_guitar.wav", "embedding": [0.1] * 3}
        ],
    }

    enriched, summary = enrich_payload(_payload_one_record(), embeddings_data=embeddings_data)

    record = enriched["files"][0]
    assert record["retrieval"]["embedding_status"] == "invalid"
    assert summary["embeddings_invalid"] == 1
```

- [ ] **步骤 4：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_marks_embedding_invalid_when_dimension_is_wrong -v
```

预期：FAIL，当前代码会标记为 `ready`。

- [ ] **步骤 5：实现 `_is_valid_embedding()`**

在 `audio_metadata/enrichment.py` 增加：

```python
def _is_valid_embedding(item: dict[str, Any], expected_dim: int = EMBEDDING_DIM_DEFAULT) -> bool:
    embedding = item.get("embedding")
    if not isinstance(embedding, list):
        return False
    if len(embedding) != expected_dim:
        return False
    return all(isinstance(value, (int, float)) and not isinstance(value, bool) for value in embedding)
```

在匹配到 item 后先检查：

```python
if not _is_valid_embedding(item):
    record["retrieval"]["embedding_status"] = "invalid"
    record["retrieval"]["embedding_model"] = model_name
    summary["embeddings_invalid"] += 1
    continue
```

- [ ] **步骤 6：编写失败测试：filename fallback 匹配**

```python
def test_enrich_matches_embedding_by_filename_fallback():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {"path": "/mnt/d/samples/warm_guitar.wav", "embedding": [0.1] * 512}
        ],
    }

    enriched, summary = enrich_payload(_payload_one_record(), embeddings_data=embeddings_data)

    record = enriched["files"][0]
    assert record["retrieval"]["embedding_status"] == "ready"
    assert summary["embeddings_ready"] == 1
```

- [ ] **步骤 7：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_matches_embedding_by_filename_fallback -v
```

预期：FAIL，当前只支持完整 path。

- [ ] **步骤 8：实现 filename fallback**

在 `audio_metadata/enrichment.py` 中增强 lookup：

```python
from pathlib import Path


def _build_embedding_lookups(embeddings_data: dict[str, Any]) -> tuple[dict[str, dict[str, Any]], dict[str, dict[str, Any]]]:
    by_path: dict[str, dict[str, Any]] = {}
    by_filename: dict[str, dict[str, Any]] = {}
    for item in embeddings_data.get("files", []):
        if not isinstance(item, dict):
            continue
        path = item.get("path")
        if isinstance(path, str):
            by_path[path] = item
            by_filename.setdefault(Path(path).name, item)
    return by_path, by_filename


def _record_filename(record: dict[str, Any]) -> str | None:
    source = record.get("source") if isinstance(record.get("source"), dict) else {}
    file_name = source.get("file_name")
    return file_name if isinstance(file_name, str) else None
```

匹配时：

```python
by_path, by_filename = _build_embedding_lookups(embeddings_data)
path = _record_path(record)
filename = _record_filename(record)
item = by_path.get(path or "") or by_filename.get(filename or "")
```

- [ ] **步骤 9：运行 enrichment 测试**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py -v
```

预期：全部 PASS。

- [ ] **步骤 10：Commit**

```bash
git add audio_metadata/enrichment.py tests/test_enrichment.py
git commit -m "feat(enrichment): 完善 embedding 匹配与校验"
```

---

## 任务 4：实现 semantic vocabulary 打分

**文件：**

- 修改：`audio_metadata/enrichment.py`
- 修改：`tests/test_enrichment.py`
- 创建：`config/semantic_tags.json`

- [ ] **步骤 1：编写失败测试：超过阈值写入 semantic tags**

```python
def test_enrich_adds_semantic_tags_above_threshold():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {"path": "audio/warm_guitar.wav", "embedding": [1.0] + [0.0] * 511}
        ],
    }
    vocabulary_data = {
        "version": "test-vocab",
        "prompts": [
            {
                "tag": "warm guitar",
                "category": "instrument_texture",
                "text": "warm guitar",
                "threshold": 0.5,
                "embedding": [1.0] + [0.0] * 511,
            }
        ],
    }

    enriched, summary = enrich_payload(
        _payload_one_record(),
        embeddings_data=embeddings_data,
        vocabulary_data=vocabulary_data,
        semantic_tags=True,
    )

    record = enriched["files"][0]
    assert record["retrieval"]["semantic_tags"] == ["warm guitar"]
    assert record["model_outputs"]["semantic_tags"] == ["warm guitar"]
    assert record["model_outputs"]["semantic_tags_confidence"]["warm guitar"] == 1.0
    assert summary["semantic_tags_added"] == 1
```

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_adds_semantic_tags_above_threshold -v
```

预期：FAIL，当前未实现 semantic tags。

- [ ] **步骤 3：实现 semantic tag scoring**

在 `audio_metadata/enrichment.py` 中引入：

```python
from audio_metadata.clap_embed import cosine_similarity
```

增加函数：

```python
def _score_semantic_tags(
    audio_embedding: list[float],
    vocabulary_data: dict[str, Any],
    *,
    default_threshold: float,
    top_n: int,
) -> dict[str, float]:
    scored: list[tuple[str, float]] = []
    for prompt in vocabulary_data.get("prompts", []):
        if not isinstance(prompt, dict):
            continue
        tag = prompt.get("tag")
        text_embedding = prompt.get("embedding")
        if not isinstance(tag, str) or not isinstance(text_embedding, list):
            continue
        if not _is_embedding_vector(text_embedding):
            continue
        score = cosine_similarity(audio_embedding, text_embedding)
        tag_threshold = prompt.get("threshold", default_threshold)
        if not isinstance(tag_threshold, (int, float)) or isinstance(tag_threshold, bool):
            tag_threshold = default_threshold
        if score >= float(tag_threshold):
            scored.append((tag, round(float(score), 3)))

    scored.sort(key=lambda item: item[1], reverse=True)
    return dict(scored[:top_n])


def _is_embedding_vector(value: Any, expected_dim: int = EMBEDDING_DIM_DEFAULT) -> bool:
    if not isinstance(value, list) or len(value) != expected_dim:
        return False
    return all(isinstance(item, (int, float)) and not isinstance(item, bool) for item in value)
```

在 `ready` 分支中增加：

```python
if semantic_tags and vocabulary_data:
    tag_scores = _score_semantic_tags(
        item["embedding"],
        vocabulary_data,
        default_threshold=threshold,
        top_n=top_n,
    )
    if tag_scores:
        tags = list(tag_scores.keys())
        record["retrieval"]["semantic_tags"] = tags
        record["model_outputs"]["semantic_tags"] = tags
        record["model_outputs"]["semantic_tags_confidence"] = tag_scores
        record["model_outputs"]["classifier_version"] = "v0.1-b7-enrichment"
        record["model_outputs"]["classifier_type"] = "hybrid_metadata_enrichment"
        summary["semantic_tags_added"] += len(tags)
```

- [ ] **步骤 4：编写失败测试：低于阈值不写入**

```python
def test_enrich_skips_semantic_tags_below_threshold():
    embeddings_data = {
        "model": "laion-audioclip-full-2022",
        "embedding_dim": 512,
        "files": [
            {"path": "audio/warm_guitar.wav", "embedding": [1.0] + [0.0] * 511}
        ],
    }
    vocabulary_data = {
        "version": "test-vocab",
        "prompts": [
            {
                "tag": "dark pad",
                "category": "role_texture",
                "text": "dark pad",
                "threshold": 0.9,
                "embedding": [0.0, 1.0] + [0.0] * 510,
            }
        ],
    }

    enriched, summary = enrich_payload(
        _payload_one_record(),
        embeddings_data=embeddings_data,
        vocabulary_data=vocabulary_data,
        semantic_tags=True,
    )

    record = enriched["files"][0]
    assert record["retrieval"]["semantic_tags"] == []
    assert record["model_outputs"]["semantic_tags"] == []
    assert summary["semantic_tags_added"] == 0
```

- [ ] **步骤 5：运行 semantic tag 测试**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py::test_enrich_adds_semantic_tags_above_threshold tests/test_enrichment.py::test_enrich_skips_semantic_tags_below_threshold -v
```

预期：PASS。

- [ ] **步骤 6：创建默认 vocabulary 文件**

创建 `config/semantic_tags.json`：

```json
{
  "version": "v0.1-b7-default",
  "prompts": [
    {"tag": "guitar", "category": "instrument", "text": "guitar", "threshold": 0.25},
    {"tag": "piano", "category": "instrument", "text": "piano", "threshold": 0.25},
    {"tag": "drums", "category": "instrument", "text": "drums", "threshold": 0.25},
    {"tag": "bass", "category": "instrument", "text": "bass", "threshold": 0.25},
    {"tag": "synth", "category": "instrument", "text": "synthesizer", "threshold": 0.25},
    {"tag": "vocal", "category": "instrument", "text": "vocal", "threshold": 0.25},
    {"tag": "warm guitar", "category": "instrument_texture", "text": "warm guitar", "threshold": 0.25},
    {"tag": "dark pad", "category": "role_texture", "text": "dark ambient pad", "threshold": 0.25},
    {"tag": "bright synth lead", "category": "role_texture", "text": "bright synthesizer lead", "threshold": 0.25},
    {"tag": "energetic drum loop", "category": "role_texture", "text": "energetic drum loop", "threshold": 0.25}
  ]
}
```

注意：默认文件不带 embedding。真实 semantic tag 写入需要 CLI 或后续任务生成 text embedding cache。MVP 单元测试使用带 embedding 的 vocabulary。

- [ ] **步骤 7：运行全部 enrichment 测试**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py -v
```

预期：全部 PASS。

- [ ] **步骤 8：Commit**

```bash
git add audio_metadata/enrichment.py tests/test_enrichment.py config/semantic_tags.json
git commit -m "feat(enrichment): 添加受控 semantic tag 打分"
```

---

## 任务 5：实现 `enrich_cli.py`

**文件：**

- 创建：`enrich_cli.py`
- 创建：`tests/test_enrich_cli.py`

- [ ] **步骤 1：编写失败测试：dry-run 不写输出文件**

创建 `tests/test_enrich_cli.py`：

```python
import json
from pathlib import Path

from enrich_cli import build_parser, run


def _write_library(path: Path) -> None:
    path.write_text(
        json.dumps({
            "schema_version": 1,
            "app_version": "test",
            "run": {},
            "files": [
                {
                    "id": "rec-1",
                    "status": "ok",
                    "source": {
                        "path": "audio/warm_guitar.wav",
                        "file_name": "warm_guitar.wav",
                        "file_format": "wav",
                    },
                }
            ],
        }),
        encoding="utf-8",
    )


def test_enrich_cli_dry_run_does_not_write_output(tmp_path):
    input_path = tmp_path / "library.json"
    output_path = tmp_path / "library.enriched.json"
    _write_library(input_path)

    parser = build_parser()
    args = parser.parse_args([
        "--input", str(input_path),
        "--output", str(output_path),
        "--dry-run",
    ])

    exit_code = run(args)

    assert exit_code == 0
    assert not output_path.exists()
```

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py::test_enrich_cli_dry_run_does_not_write_output -v
```

预期：FAIL，失败原因是 `enrich_cli` 模块不存在。

- [ ] **步骤 3：实现最少 CLI**

创建 `enrich_cli.py`：

```python
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path
from typing import Any

from audio_metadata.enrichment import enrich_payload


def build_parser(add_help: bool = True) -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        description="Enrich audio metadata with embedding coverage and semantic tags.",
        add_help=add_help,
    )
    parser.add_argument("--input", required=True, help="Input library JSON file.")
    parser.add_argument("--output", required=True, help="Output enriched library JSON file.")
    parser.add_argument("--embeddings", help="Optional CLAP embeddings JSON file.")
    parser.add_argument("--vocabulary", help="Optional semantic vocabulary JSON file.")
    parser.add_argument("--semantic-tags", action="store_true", help="Enable semantic tag scoring.")
    parser.add_argument("--threshold", type=float, default=0.25, help="Default semantic tag threshold.")
    parser.add_argument("--top-n", type=int, default=5, help="Max semantic tags per record.")
    parser.add_argument("--dry-run", action="store_true", help="Print summary without writing output.")
    parser.add_argument("--force", action="store_true", help="Overwrite output if it exists.")
    parser.add_argument("--verbose", "-v", action="store_true", help="Print verbose summary.")
    return parser


def validate_args(args: argparse.Namespace, parser: argparse.ArgumentParser) -> None:
    if args.top_n <= 0:
        parser.error("--top-n must be greater than 0.")
    if args.threshold < -1.0 or args.threshold > 1.0:
        parser.error("--threshold must be between -1.0 and 1.0.")


def run(args: argparse.Namespace) -> int:
    try:
        input_path = Path(args.input)
        output_path = Path(args.output)

        payload = _read_json(input_path)
        embeddings_data = _read_json(Path(args.embeddings)) if args.embeddings else None
        vocabulary_data = _read_json(Path(args.vocabulary)) if args.vocabulary else None

        enriched, summary = enrich_payload(
            payload,
            embeddings_data=embeddings_data,
            vocabulary_data=vocabulary_data,
            semantic_tags=args.semantic_tags,
            threshold=args.threshold,
            top_n=args.top_n,
        )

        _print_summary(summary, output_path)

        if args.dry_run:
            return 0

        if output_path.exists() and not args.force:
            print(f"Error: output file already exists: {output_path}. Use --force to overwrite.", file=sys.stderr)
            return 1

        _write_json_atomic(output_path, enriched)
        return 0
    except ValueError as exc:
        print(f"Error: {exc}", file=sys.stderr)
        return 1


def _read_json(path: Path) -> dict[str, Any]:
    try:
        return json.loads(path.read_text(encoding="utf-8-sig"))
    except OSError as exc:
        raise ValueError(f"Could not read JSON file {path}: {exc}") from exc
    except json.JSONDecodeError as exc:
        raise ValueError(f"Invalid JSON in {path}: {exc.msg} (line {exc.lineno}, column {exc.colno})") from exc


def _write_json_atomic(path: Path, data: dict[str, Any]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    tmp_path = path.with_suffix(path.suffix + ".tmp")
    tmp_path.write_text(json.dumps(data, indent=2, ensure_ascii=False), encoding="utf-8")
    tmp_path.replace(path)


def _print_summary(summary: dict[str, Any], output_path: Path) -> None:
    print("Enrichment summary:")
    print(f"  records: {summary.get('records', 0)}")
    print(f"  embeddings ready: {summary.get('embeddings_ready', 0)}")
    print(f"  embeddings missing: {summary.get('embeddings_missing', 0)}")
    print(f"  embeddings invalid: {summary.get('embeddings_invalid', 0)}")
    print(f"  embeddings not requested: {summary.get('embeddings_not_requested', 0)}")
    print(f"  semantic tags added: {summary.get('semantic_tags_added', 0)}")
    print(f"  output: {output_path}")


if __name__ == "__main__":
    parser = build_parser()
    parsed_args = parser.parse_args()
    validate_args(parsed_args, parser)
    raise SystemExit(run(parsed_args))
```

- [ ] **步骤 4：运行 dry-run 测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py::test_enrich_cli_dry_run_does_not_write_output -v
```

预期：PASS。

- [ ] **步骤 5：编写失败测试：输出已存在且无 force 时失败**

```python
def test_enrich_cli_refuses_to_overwrite_without_force(tmp_path):
    input_path = tmp_path / "library.json"
    output_path = tmp_path / "library.enriched.json"
    _write_library(input_path)
    output_path.write_text("{}", encoding="utf-8")

    parser = build_parser()
    args = parser.parse_args([
        "--input", str(input_path),
        "--output", str(output_path),
    ])

    exit_code = run(args)

    assert exit_code == 1
    assert json.loads(output_path.read_text(encoding="utf-8")) == {}
```

- [ ] **步骤 6：运行测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py::test_enrich_cli_refuses_to_overwrite_without_force -v
```

预期：PASS。如果失败，修正覆盖保护逻辑。

- [ ] **步骤 7：编写失败测试：正常写入 enriched JSON**

```python
def test_enrich_cli_writes_enriched_json(tmp_path):
    input_path = tmp_path / "library.json"
    output_path = tmp_path / "library.enriched.json"
    _write_library(input_path)

    parser = build_parser()
    args = parser.parse_args([
        "--input", str(input_path),
        "--output", str(output_path),
    ])

    exit_code = run(args)

    assert exit_code == 0
    data = json.loads(output_path.read_text(encoding="utf-8"))
    assert data["files"][0]["retrieval"]["embedding_status"] == "not_requested"
```

- [ ] **步骤 8：运行 CLI 测试**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py -v
```

预期：全部 PASS。

- [ ] **步骤 9：Commit**

```bash
git add enrich_cli.py tests/test_enrich_cli.py
git commit -m "feat(cli): 添加 enrich 命令实现"
```

---

## 任务 6：在 `app.py` 注册 enrich 子命令

**文件：**

- 修改：`app.py`
- 修改：`tests/test_enrich_cli.py`

- [ ] **步骤 1：编写失败测试：app parser 包含 enrich**

在 `tests/test_enrich_cli.py` 增加：

```python
def test_app_parser_accepts_enrich_command(tmp_path):
    from app import build_parser

    input_path = tmp_path / "library.json"
    output_path = tmp_path / "library.enriched.json"

    parser = build_parser()
    args = parser.parse_args([
        "enrich",
        "--input", str(input_path),
        "--output", str(output_path),
    ])

    assert args.command == "enrich"
    assert args.handler is not None
```

- [ ] **步骤 2：运行测试验证失败**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py::test_app_parser_accepts_enrich_command -v
```

预期：FAIL，当前 `app.py` 不认识 `enrich`。

- [ ] **步骤 3：修改 `app.py` 注册命令**

在 import 区增加：

```python
import enrich_cli as enrich_command
```

在 `build_parser()` 中增加：

```python
enrich_parser = subparsers.add_parser(
    "enrich",
    help="Enrich metadata with embedding coverage and semantic tags (v0.1-b7).",
    parents=[enrich_command.build_parser(add_help=False)],
)
enrich_parser.set_defaults(
    handler=enrich_command.run,
    validator=enrich_command.validate_args,
)
```

建议放在 `auto-tag` 之后、`compute-embeddings` 之前。

- [ ] **步骤 4：运行测试验证通过**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrich_cli.py::test_app_parser_accepts_enrich_command -v
```

预期：PASS。

- [ ] **步骤 5：运行 app help 手动验证**

运行：

```bash
.venv/bin/python app.py --help
```

预期：help 中出现 `enrich`。

- [ ] **步骤 6：Commit**

```bash
git add app.py tests/test_enrich_cli.py
git commit -m "feat(app): 注册 enrich 子命令"
```

---

## 任务 7：补充文档并更新当前目标

**文件：**

- 修改：`README.md`
- 修改：`CURRENT_GOAL.md`
- 已有：`docs/v0.1-b7-metadata-enrichment-design.md`

- [ ] **步骤 1：更新 `CURRENT_GOAL.md`**

将当前目标更新为：

```markdown
## Version
- Current repo version: `v0.1-b7` 🚧 IN PROGRESS
- Previous version: `v0.1-b6` (CLAP Semantic Search) ✅
- Current milestone: `v0.1-b7` (Metadata Enrichment Layer)
```

并列出 Phase：

```markdown
## v0.1-b7 Deliverables

### Phase 1: Schema + Enrichment Core
- [ ] Extend retrieval/model_outputs fields
- [ ] Add embedding coverage status
- [ ] Preserve schema v1 compatibility

### Phase 2: Semantic Vocabulary Tags
- [ ] Add controlled semantic vocabulary
- [ ] Score audio embeddings against vocabulary embeddings
- [ ] Write semantic tags with confidence

### Phase 3: CLI Integration
- [ ] Add `app.py enrich`
- [ ] Add dry-run / force behavior
- [ ] Document recommended workflow
```

- [ ] **步骤 2：更新 `README.md` 能力列表**

在能力列表中增加：

```markdown
- **`enrich`: combine indexed metadata, embedding coverage, and controlled semantic tags into an enriched library JSON (v0.1-b7 new)**
```

增加使用示例：

```markdown
### Enrich Metadata (v0.1-b7 new)

```bash
python app.py enrich \
  --input ./out/library.json \
  --embeddings ./out/embeddings.json \
  --vocabulary ./config/semantic_tags.json \
  --semantic-tags \
  --output ./out/library.enriched.json
```

`enrich` 不会把 512 维 CLAP embedding 写入 `library.enriched.json`，只写入 embedding 状态、引用和 semantic tags。
```
```

- [ ] **步骤 3：运行文档相关检查**

运行：

```bash
git diff -- README.md CURRENT_GOAL.md docs/v0.1-b7-metadata-enrichment-design.md
```

预期：确认只改文档，无意外删除大段内容。

- [ ] **步骤 4：Commit**

```bash
git add README.md CURRENT_GOAL.md docs/v0.1-b7-metadata-enrichment-design.md
git commit -m "docs(v0.1-b7): 添加元数据增强层设计说明"
```

---

## 任务 8：全量验证与收尾

**文件：**

- 可能修改：测试或文档中的小问题

- [ ] **步骤 1：运行新增测试**

运行：

```bash
.venv/bin/python -m pytest tests/test_enrichment.py tests/test_enrich_cli.py -v
```

预期：全部 PASS。

- [ ] **步骤 2：运行全量测试**

运行：

```bash
.venv/bin/python -m pytest -v
```

预期：全部 PASS。若测试环境缺少可选 CLAP 依赖，CLAP 相关测试应使用 mock 或跳过真实模型加载。

- [ ] **步骤 3：运行实际 CLI smoke test**

创建临时目录并运行：

```bash
mkdir -p /tmp/amm-enrich-smoke
cat > /tmp/amm-enrich-smoke/library.json <<'JSON'
{
  "schema_version": 1,
  "app_version": "smoke",
  "run": {},
  "files": [
    {
      "id": "rec-1",
      "status": "ok",
      "source": {
        "path": "audio/warm_guitar.wav",
        "file_name": "warm_guitar.wav",
        "file_format": "wav"
      }
    }
  ]
}
JSON
.venv/bin/python app.py enrich \
  --input /tmp/amm-enrich-smoke/library.json \
  --output /tmp/amm-enrich-smoke/library.enriched.json
```

预期：退出码 0，输出 summary，生成 `library.enriched.json`。

- [ ] **步骤 4：检查生成结果**

运行：

```bash
.venv/bin/python - <<'PY'
import json
from pathlib import Path
p = Path('/tmp/amm-enrich-smoke/library.enriched.json')
data = json.loads(p.read_text(encoding='utf-8'))
record = data['files'][0]
assert record['retrieval']['embedding_status'] == 'not_requested'
print('smoke ok')
PY
```

预期：输出 `smoke ok`。

- [ ] **步骤 5：检查 git 状态**

运行：

```bash
git status --short
```

预期：没有未提交的 v0.1-b7 修改。仓库中已有的 `tests/run_v0.1-b6_tests.py` 若仍为未跟踪文件，单独报告，不混入本次提交。

- [ ] **步骤 6：最终收尾 commit（如有小修）**

```bash
git add <changed-files>
git commit -m "test(v0.1-b7): 验证元数据增强流程"
```

---

## 代码审查重点

### 安全

- `enrich_cli.py` 必须默认拒绝覆盖输出文件。
- JSON 写入必须先写 `.tmp`，再 `replace()`。
- 不允许根据 vocabulary 中的任意字段执行代码或 shell。

### 异常处理

- 输入 JSON 损坏时返回清晰错误。
- 单条 embedding 无效时只标记 `invalid`，不让整个流程失败。
- 未提供 embeddings 时应正常输出 `not_requested`。

### 性能

- 不要在 `enrich_payload()` 中加载 CLAP 模型。
- vocabulary scoring 只对 `ready` embedding 执行。
- 对 MVP 使用线性扫描即可，暂不引入向量库。

## 完成定义

完成 v0.1-b7 MVP 必须满足：

- `app.py enrich` 可用。
- 新增单元测试与 CLI 测试通过。
- 全量测试不回归。
- 文档说明清楚新命令、数据结构和非目标。
- 自动 semantic tags 来自受控 vocabulary，不自由生成。
- `library.enriched.json` 不包含 512 维 audio embedding。
